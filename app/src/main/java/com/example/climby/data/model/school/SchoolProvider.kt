package com.example.climby.data.model.school

import com.example.climby.data.model.province.ProvinceModel

class SchoolProvider {
    companion object {
        var schools: List<SchoolModel> = emptyList()
    }
}
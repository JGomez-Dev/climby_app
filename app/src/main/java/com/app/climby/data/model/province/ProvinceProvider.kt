package com.app.climby.data.model.province

import com.app.climby.data.model.trip.TripModel

class ProvinceProvider {
    companion object {
        var provinces: List<ProvinceModel> = emptyList()
    }
}